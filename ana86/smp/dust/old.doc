９．９  @part

      これはライブラリ等を作成するのに一つのファイルをいくつかのファ
    イルに分割するためのもので、手続き外で、

    	@part ファイル名

    を指定すると、それまでの出力をクローズして、このファイル名のファ
    イルに出力するようにします（ファイル名は""で括ってはいけません）。
      この機能を使うには処理系のコマンドラインでオプションを指定する
    必要があります。
      分割すると、作成されたファイル一覧を収めたファイル、importした
    手続き名や変数名を収めたファイル、ライブラリ作成のためのファイル、
    
  	モジュール名.l
      	モジュール名.inc
    	モジュール名.rsp

    というファイルを生成します。

      .inc, .rsp ファイルはそのまま利用できますが、モジュール.l はど
    う利用できるか、あやしいです。
      実験的に付けただけなので、分割出力後のソースのアセンブルをどう
    するか、とか、結構めんどくさいです^^;

      分割すると当然、別別にアセンブルされるので、分割したファイル間
    で使われる手続き名、変数名はたとえ輸出するべきものでなくても '*'
    で輸出するようにしてください。

      そのた、分割するとなんらかの副作用があるので気をつけてください。
    （^^;）


*****
    ※  現在、far call (export) される手続きで、その手続き内で gosub
	によるサブルーチンコールで ret を使うと, 生成後、masm の
	proc ～ endp の間に置かれるため、near ret にならず far ret にな
	ってしまいますので、このばあいの使用は避けてください。


  □  インターフェース部
      インターフェース部には
	far
    が指定できます（後述）。
      また、以下のような命令を予約しています。
        in    ﾚｼﾞｽﾀ1,ﾚｼﾞｽﾀ2,……
        out   ﾚｼﾞｽﾀ1,ﾚｼﾞｽﾀ2,……
        break ﾚｼﾞｽﾀ1,ﾚｼﾞｽﾀ2,……
    これは手続きのレジスタの入出力の状態を表そうというものですが、現在
    これらが指定されるとエラーにならずコメントのようなもんですが、間違
    った記述をしてたばあい単なるコメントよりもたちがわるそなので使わな
    いでください。
      ということで(?), このインタフェース部に手続きの説明やレジスタの
    入出力をコメントしとくとよいかもしれません。



  □  呼び出し前処理（マクロ）部
      引数処理を行ったあと、手続き呼び出しを行うまえに行う命令を指定
    します。指定は、
        before
	    (命令群)
    で、begin,enter,endproc の何れかが置かれるまで有効です。
      また、before の変わりに macro を用いて
    	proc 手続き名(..)
    	macro
    	    (命令群)
    	endproc
    とすると、手続き呼び出しの call 命令を生成せず、ただのマクロになり
    ます。
      ただし、(命令群) として使える命令はかなり限定され、基本的に式表現
    の命令(rol,com等も含む), push/pop, intr 等で、ジャンプ・コール関係
    （制御命令）やストリング命令は指定できません。
      また、手続き引き数にマクロのためのパラメータを指定することはでき
    ませんし、引き数のローカルラベルを指定することもできません。

