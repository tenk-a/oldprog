ana86 v0.18


  0.17の虫取りです。もうちょい変更しようと思ってたらいつになるか怪しい
ので、とりあえず^^;
  実行ファイル ana.exe(v0.17)のbdiff差分ファイルです。
  bupdate を用いてバージョン・アップしてください。


■  v0.17->v0.18での変更

・配列で、0x8000以上のサイズを指定すると、生成されるasmソースでは
  負の値になってしまっていた。

・dx.ax = -1 など、４バイトレジスタに負の値を代入するとき、上位ワード
  が０になってしまった。



■  v0.17->v0.18の間で見つかったけど対応していないバグや仕様(注意点)

・&VarName のように使われたとき、VarName が定義されているかどうかチェック
  していない。

・import した cvar 変数は、例えスモールモデルであっても csでセグメント・
  オーバーライドされない... というか、cvar で定義した変数は輸出してはいけ
  ない仕様でした^^;。

・同一モジュールを分割することは今のところ言語仕様ではサポートしておらず、
  c_nameでモジュール名を調節して胡麻かして（コンパイラを騙して）行なうだ
  けなので、例え実際は同一モジュールのルーチンでもファイルが違えば別の
  モジュールであることに気を付けて、手続き名や変数名を指定しないといけない。
